apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ../../base
secretGenerator:
  - name: wazuh-auth
    envs:
      - secrets/wazuh-auth.env
generatorOptions:
  disableNameSuffixHash: true
